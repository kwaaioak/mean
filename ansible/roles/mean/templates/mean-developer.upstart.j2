description "Mean Developer Service"

start on runlevel [2345]
stop on runlevel [!2345]

setuid {{ mean_username }}

respawn

limit nofile 8192 8192

env NODE_ENV={{ mean_node_env }}

script
    cd {{ mean_src_dir }}
    exec gulp
end script
